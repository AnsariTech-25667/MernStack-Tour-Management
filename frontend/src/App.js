/**
 * App.js
 * Entry point for the frontend demo UI.
 * This file is intentionally tiny; replace with real SPA components later.
 */
import "./App.css";

function App() {
  return (
    <div className="app-container">
      <h1>Tour Management — Demo</h1>
      <p>Frontend scaffold (React). Build UI components under <code>src/</code>.</p>
    </div>
  );
}

export default App;
